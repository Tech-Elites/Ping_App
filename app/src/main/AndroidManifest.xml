<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ping">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ping">
        <activity android:name=".ViewPing"></activity>
        <activity android:name=".EachPersonAccount" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".SeeOnMap"
            android:label="@string/title_activity_see_on_map" />
        <activity
            android:name=".LandingPage"
            android:label=""
             />
        <activity
            android:name=".RegisterPage"
            android:label="" />
        <activity
            android:name=".LoginPage"
            android:label="" />
        <activity
            android:name=".PingConfirmNewPing_PingBacks"
            android:label="" />
        <activity
            android:name=".PingConfirmPingBack"
            android:label="" />
        <activity
            android:name=".PingConfirmNewPing"
            android:label="" />
        <activity
            android:name=".PingBackForm"
            android:label="" />
        <activity
            android:name=".ViewOnMap"
            android:label="" />
        <activity
            android:name=".EachPing"
            android:label="" />
        <activity android:name=".NotificationPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>