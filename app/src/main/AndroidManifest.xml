<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ping">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ping">
        <activity android:name=".LandingPage" android:label=""></activity>
        <activity android:name=".RegisterPage"  android:label=""></activity>
        <activity android:name=".LoginPage" android:label=""/>
        <activity android:name=".PingConfirmNewPing_PingBacks" android:label=""/>
        <activity android:name=".PingConfirmPingBack" android:label=""/>
        <activity android:name=".PingConfirmNewPing" android:label=""/>
        <activity android:name=".PingBackForm" android:label=""/>
        <activity android:name=".ViewOnMap" android:label=""/>
        <activity android:name=".EachPing" android:label=""/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>